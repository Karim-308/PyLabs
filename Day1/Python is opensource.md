#Python
Open source · General purpose · Fast to deploy
## Case Sensitivity
Python is case sensitive — Name and name are different variables.
## Variables
No var or let keywords. Just assign directly:
pythonname = "Alice"
salary = 5000
## Indentation
Python uses indentation instead of curly brackets {} to define blocks.

## F-strings / Template Strings
pythonemail = '''
Hi {name},
your salary is {salary}
'''
## Loop Example
pythonfor i in range(len(names)):
    print(email.format(name=names[i], salary=salaries[i]))

### Note: range() takes len(names), not range of (names). Also, .format() uses keyword arguments matching the placeholders in the string.

number1 = 1
 number2 = 2 
 number3  = "this is a string"

array = [number1, number2, number3]

print(type(number1))
print(type(number2))
print(type(number3))
print(type(array))

# typecasting 

number1 =11
age = int (number1)
smytring = string(number1)
ystring2  = "1.7a"
myflaot = float (mysting2)#shouuldn;y work
myint = int (mysting2)#shouldn't work

# Operators
z = x / y
res = 166/5
print (res)


# Truthy and falsy values

True == 1
False == 0
True == 2
False = "false"
2 == '2'


# Logic gates

2 and 1
2 or 1
not 4
not 0
2 and 0
0 and2
'Google' adn 1
"" and 'go'
False or 0

# String oprations

